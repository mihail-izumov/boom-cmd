// Список бизнесов приложения (переключатель в чипе шапки Главной).
//
// Сейчас бизнес ровно один — «Бумбастик», он же активный. Реальной
// мультибизнесовости НЕТ и в этой задаче не делается: список — витрина
// текущего состояния, а не механика переключения.
//
// `chip` — подпись в бейдже шапки (капсом, как было), `name` — как бизнес
// называется в списке. Разделены намеренно: в бейдже капс — это стиль
// (uppercase живёт в классе), а не написание имени.

export const BUSINESSES = [
  { id: 'bumbastik', name: 'Бумбастик', chip: 'БУМБАСТИК', active: true },
]

export const ACTIVE_BUSINESS =
  BUSINESSES.find((b) => b.active) || BUSINESSES[0] || null
