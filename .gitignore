# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-*/
dist-ssr
*.local

# Временная папка verify-daily (чистится сама при успешном прогоне;
# прерванный прогон оставляет её — в коммит не берём)
.tmp-verify-daily/

# Локальные env: URL источников (VITE_PROJECTS_API, VITE_ANALYTICS_API).
# Фраза доступа здесь НЕ хранится (она только в localStorage).
# Покрыто *.local выше — дублируем явно для ясности.
.env.local
.env*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# База знаний / внутренние брифы — не коммитим в публичный репозиторий.
# Файлы остаются локально и доступны ИИ-ассистентам. Если репозиторий приватный
# и документацию нужно версионировать — удали строку ниже.
docs/
